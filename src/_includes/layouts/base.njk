<!doctype html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{ title or metadata.title }}</title>
    <meta name="description" content="{{ description or metadata.description }}">
    <link rel="stylesheet" href="{{ '/css/index.css' | url }}">
    <link rel="alternate" href="{{ metadata.feed.path | url }}" type="application/atom+xml" title="{{ metadata.title }}">
    <link rel="alternate" href="{{ metadata.jsonfeed.path | url }}" type="application/json" title="{{ metadata.title }}">

    <!-- Open Graph -->
    <meta property="og:title" content="{{ title or metadata.title }}" />
    <meta property="og:type" content="website" />
    <meta property="og:url" content="{{ metadata.url }}" />

    <!-- Twitter Card -->
    <meta name="twitter:site" content="{{ metadata.url }}" />
    <meta name="twitter:creator" content="@tolleration" />
    <meta property="og:url" content="{{ metadata.url }}}" />
  </head>
  <body>

    <header>
      <section class="header">
        <a class="home" href="{{ '/' | url }}"><span class="site-name">{{ metadata.title }}</span>.</a>
        <nav>
          <a href="{{ '/about' | url }}">Sobre</a>
          <a href="{{ '/posts' | url }}">Posts</a>
          <a href="{{ '/search' | url }}">Pesquisar</a>
          <a href="{{ '/feed/feed.xml' | url }}">RSS</a>
        </nav>
      </section>
    </header>

    <main{% if templateClass %} class="{{ templateClass }}"{% endif %}>
      {{ content | safe }}
    </main>

    <footer>
      <section class="footer">
        <section>
          <p><a href="{{ '/about' | url }}">Sobre</a></p>
          <p><a href="https://www.youtube.com/channel/UC1QrvNdWT54j6Sz7Bx4wDWg">Canal no Youtube</a></p>
        </section>

        <section>
          <p>Me encontre no <a href="https://bolha.us/@tolleration">Mastodon</a> ou via <a href="mailto:laraftoller@gmail.com">email</a>.</p>

          <p>
            <small>Since 2021. Este website e seus trabalhos estão sob a licença <a href="http://https://creativecommons.org/licenses/by-sa/4.0/">CC BY-SA 4.0</a>, caso não especificado de outra forma.
            </small>
          </p>

        </section>

      </section>
    </footer>
  </body>
</html>
